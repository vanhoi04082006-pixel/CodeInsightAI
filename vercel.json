{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "prisma generate --schema prisma/schema.prod.prisma && next build",
  "regions": ["sin1"],
  "github": {
    "silent": true
  }
}
