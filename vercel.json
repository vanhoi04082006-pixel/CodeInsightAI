{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "prisma generate --schema prisma/schema.prod.prisma && prisma db push --schema prisma/schema.prod.prisma --accept-data-loss && next build",
  "regions": ["sin1"],
  "github": {
    "silent": true
  },
  "ignoreCommand": "case \"$VERCEL_GIT_COMMIT_REF\" in dependabot/*) echo 'skip dependabot preview build'; exit 0;; *) exit 1;; esac"
}
